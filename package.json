{
  "name": "htmltag",
  "version": "0.4.4",
  "description": "HTML template tag compiler",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zenparsing/htmltag.git"
  },
  "keywords": [
    "template",
    "template tag",
    "template string",
    "html"
  ],
  "homepage": "https://github.com/zenparsing/htmltag",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.15.0"
  },
  "scripts": {
    "test": "eslint src/* && node test",
    "prepublishOnly": "npm run build",
    "build": "git clean -dfX ./lib && babel src --out-dir lib --presets=es2015"
  }
}
